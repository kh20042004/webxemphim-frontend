<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FPT Play</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header class="topbar">
    <div class="topbar-left">
      <div class="logo">▶ FPT Play</div>
      <div class="search-box">
        <input type="text" placeholder="Tìm phim..." />
        <button class="search-icon">🔍</button>
      </div>
    </div>
    <div class="topbar-right">
      <div class="icon-item">PL</div>
      <div class="icon-item">SC</div>
      <div class="icon-item">GC</div>
      <div class="icon-item">👤</div>
    </div>
  </header>

  <nav class="menu">
    <div class="menu-links">
      <a href="#">Truyền hình</a>
      <a href="#">Phim bộ</a>
      <a href="#">TV show</a>
      <a href="#">Thiếu nhi</a>
      <a href="#">Thể thao</a>
    </div>
    <div class="menu-actions">
      <a class="pill promo" href="#">Phim chiếu rạp</a>
      <a class="pill vip" href="#">Gói VIP</a>
    </div>
  </nav>

  <main class="index-page">
    <% const featured = movies[0]; %>
    <section class="hero">
      <img src="<%= featured.image %>" class="hero-bg" alt="<%= featured.title %>">
      <div class="hero-overlay"></div>

      <div class="hero-content">
        <div class="hero-badge">Phim chiếu rạp</div>
        <h1 class="hero-title"><%= featured.title %></h1>
        <p class="hero-desc"><%= featured.description %></p>
        <a href="/watch/0" class="btn-play">▶ Xem Ngay</a>
      </div>

      <div class="hero-thumbs">
        <% movies.slice(1, 5).forEach((movie) => { %>
          <a href="/watch/<%= movie.id %>"><img src="<%= movie.image %>" alt="<%= movie.title %>"></a>
        <% }) %>
      </div>
    </section>
  </main>
</body>
</html>